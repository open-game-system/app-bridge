import React from 'react';
import App from '../App';

describe('<App />', () => {
  it('can be imported', () => {
    // Just check that the component can be imported
    expect(App).toBeDefined();
  });
}); 